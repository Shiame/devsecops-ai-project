name: backend CI pipeline
on:
    push:
        branches: ["main"]
jobs:
    steps:
        runs-on: ubuntu-latest
        steps:
            - name: a sample test message
            run: echo "This is my first pipeline good luck to me"
